/* Regex de email para a validação de sua string */
export const emailRegex = /^[\w\.]+@(outlook\.com|gmail\.com|yahoo\.com|hotmail\.com)$/;

/* Regex de cpf para a validação de sua string */
export const cpfRegex = /^\d{3}\d{3}\d{3}\d{2}$/;

/* Regex de password para a validação de sua string */
export const passwordRegex = /^\S{6,}$/;